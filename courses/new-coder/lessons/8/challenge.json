{
  "language": "javascript",
  "instructions": "Create `let score = 72;`. If the score is at least 70, print exactly `Passed`. Otherwise, print `Try again`.",
  "starter": "let score = 72;\n\n// Write your if/else here\n",
  "hints": [
    "Use an if statement with score >= 70.",
    "Print with console.log().",
    "For score 72, the output should be Passed."
  ],
  "tests": [
    { "id": 1, "description": "No errors in your code", "check": "console-no-error" },
    { "id": 2, "description": "Uses an if statement", "check": "code-contains", "value": "if" },
    { "id": 3, "description": "Prints Passed", "check": "console-output-equals", "value": "Passed" }
  ]
}
