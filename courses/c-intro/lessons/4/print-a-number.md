---
id: "print-a-number"
title: "Print a number"
type: "PRACTICE"
xpReward: 30
module: "C Foundations"
---

# Print a number

`printf()` uses format placeholders when printing variables.

For an integer, use `%d`:

```c
int count = 3;
printf("Count: %d\n", count);
```

The placeholder marks where the value should appear. The variable after the comma supplies the value.
