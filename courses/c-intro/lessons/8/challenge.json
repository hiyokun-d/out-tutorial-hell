{
  "instructions": "Use a for loop to add the numbers 1 through 5. Print exactly `15`.",
  "starter": "#include <stdio.h>\n\nint main() {\n    int total = 0;\n\n    // Add 1 through 5 to total\n\n    printf(\"%d\\n\", total);\n    return 0;\n}\n",
  "hints": [
    "Use for (int i = 1; i <= 5; i++).",
    "Inside the loop, write total = total + i;",
    "Print total after the loop."
  ],
  "tests": [
    { "id": 1, "description": "Prints 15", "check": "stdout-contains", "value": "15" },
    { "id": 2, "description": "Uses a for loop", "check": "code-contains", "value": "for" },
    { "id": 3, "description": "Uses total", "check": "code-contains", "value": "total" }
  ]
}
