---
id: "sum-loop-c"
title: "Sum with a loop"
type: "PRACTICE"
xpReward: 35
module: "Control Flow"
---

# Sum with a loop

A loop can build up a value over time.

```c
int total = 0;

for (int i = 1; i <= 3; i++) {
    total = total + i;
}
```

After the loop, `total` contains the final result.
