---
id: "pass-message-c"
title: "Pass message"
type: "PRACTICE"
xpReward: 30
module: "Control Flow"
---

# Pass message

A condition lets a C program choose between messages.

The shape is:

```c
if (condition) {
    // when true
} else {
    // when false
}
```

In this lab, you will use a score to print a result.
