---
id: "list-total"
title: "Total a list"
type: "PRACTICE"
xpReward: 35
module: "Data"
---

# Total a list

A common programming task is walking through a collection and building a result.

For numbers, that often means adding everything into a running total.

```python
total = 0
for number in numbers:
    total = total + number
```

The variable `total` changes each time the loop runs.
