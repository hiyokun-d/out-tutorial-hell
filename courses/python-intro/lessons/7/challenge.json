{
  "instructions": "Create `score = 68`. If the score is at least 70, print `Passed`. Otherwise, print exactly `Retry`.",
  "starter": "score = 68\n\n# Write your if/else here\n",
  "hints": [
    "Use if score >= 70:",
    "Remember to indent the print lines.",
    "With score 68, the else branch should print Retry."
  ],
  "tests": [
    { "id": 1, "description": "Prints Retry", "check": "stdout-contains", "value": "Retry" },
    { "id": 2, "description": "Uses an if statement", "check": "code-contains", "value": "if" },
    { "id": 3, "description": "Uses else", "check": "code-contains", "value": "else" }
  ]
}
