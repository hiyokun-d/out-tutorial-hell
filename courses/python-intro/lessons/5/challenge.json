{
  "instructions": "Create a variable called `name` and set it to `Maya`. Then print exactly `Hello, Maya` using that variable.",
  "starter": "# Create the variable\n\n# Print the greeting\n",
  "hints": [
    "Use name = \"Maya\".",
    "Use an f-string: print(f\"Hello, {name}\")",
    "The output must be exactly Hello, Maya."
  ],
  "tests": [
    { "id": 1, "description": "Prints Hello, Maya", "check": "stdout-contains", "value": "Hello, Maya" },
    { "id": 2, "description": "Uses a name variable", "check": "code-contains", "value": "name" },
    { "id": 3, "description": "Uses print()", "check": "code-contains", "value": "print" }
  ]
}
